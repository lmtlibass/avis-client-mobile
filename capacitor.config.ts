import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend_appsatisfaction',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
